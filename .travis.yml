language: ruby
rvm:
  - "1.9.3"
install:
  - sudo apt-get update
  - sudo apt-get -y install linux-headers-$(uname -r)
  - sudo apt-get -y install virtualbox vagrant
  - VBoxManage -v
script:
  - date
  - vagrant up
  - echo "Box build"
